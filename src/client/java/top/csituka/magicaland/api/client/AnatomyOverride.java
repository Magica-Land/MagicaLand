package top.csituka.magicaland.api.client;

/** Temporary horn/wing presence supplied by an addon; does not change saved appearance. Since API 1.4. */
public record AnatomyOverride(boolean hasHorn, boolean hasWings) {}
