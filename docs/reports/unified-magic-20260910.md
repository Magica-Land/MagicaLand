# 2026-09-10 光效统一、远控瞄准与悬浮前小腿

## 本轮变更

- 角、悬浮物品、拖尾、包身光改用同类宽幅不规则光纹。物品保留实际纹理透明剪影，星星保留现有数量和节奏。
- 空手出窍显示柔光核与收尖焰尾。尾部随实体实际移动反向延展，运动按 tick 采样，传送或换世界清理。
- Gameplay 的 Mod Menu 设置页提供能力视角：自动背面第三人称／沿用发动前本体视角。只在接管相机时应用一次，F5 和返回本体的视角恢复保留。设置独立保存，并只读迁移此前放在外观配置中的选择。
- 第三人称持物向观察镜头侧边偏移 0.30 格，左手镜像。准星显示原交互射线的屏幕落点，冷却条随准星移动；实体、碰撞、射线和服务器交互规则不变。
- 按用户最新确认，前腿恢复整条蜷曲，使用原姿态的 95% 旋转和同倍率位置补偿；普通无翼悬浮从腿根放低约 30°，冲刺平滑抬回原高度。升降惯性只在根部附加最多 ±4°，后腿维持原规则。取消此前的前两节 35% 和蹄部独立反解实验。

## 资源与版本

外观开发版本 0.3.3，公共 API 1.3；Gameplay 开发版本 0.2.2，要求外观 `>=0.3.3 <0.4.0`。新增 `AppearanceVisuals.renderFlame`，Gameplay 仍只依赖公开 API。能力视角设置、按钮和配置测试已移到 Gameplay；删除尚未发布的外观偏好接口及对应桥接，外观 F9 保持原有设置。

飞行来源为 `src/main/resources/assets/magicaland/animations/mare_animation.json` 的 `fly` 第一帧。`internal.levitate` 由代码派生，没有单独的动画资源。前小腿为 `LFrontCalf`、`RFrontCalf`，倍率位于 `PonyFlightMotion`。本轮未改模型、UV、贴图、Blockbench 工程或动画 JSON；修改 `fly` 会同时影响有翼飞行。

前腿旋转与位置补偿使用同一倍率；测试不代替实机检查接缝。

## 已完成的独立检查

- 最新两包实际源码 Java 17 类型检查：外观 143 个源文件，玩法 51 个源文件；玩法仅使用编译出的 API 分类依赖。
- 最新公开 API：107 项及仅 API 产物编译外部调用示例；跨仓库内部依赖检查通过。外观架构检查 506 项。
- 最新设置保存/UI 处理器 380 项：真实 Gson、实际设置页和 Mod Menu 入口，覆盖提前只读迁移、外观随后重存也保留选择、专用服务端不生成客户端配置、独立文件优先、未知字段保留、即时保存、失败提示及重试、返回原页面。玩法结构 110 项、架构 296 项通过。
- 最新蜷腿方案：飞行状态 109,416 项、派生动画 1,931 项、绘制恢复 386,351 项。核对实际 mare/changeling 模型的两侧降低方向、前腿连接体积、升降和冲刺插值。头部、悬浮惯性、装备进出场与音效状态回归通过。
- 此前瞄准投影 13,162 项、物品姿态 35,441 项、远控状态 283 项通过；这些功能本次未改。
- 真实 Minecraft ShaderProgram、Framebuffer、Fabulous RGBA8 透明合成与原版暗角的 198 项回归通过，包含效果启停、缓冲复用、尺寸切换及最终画面提交。
- 光焰运动／几何 4,479 项；实际角／光焰 shader 3,018 项、点亮 133,783 项、物品 15 项、绘制顺序 16 项、拖尾 97 项、云层 33 项、包身 452 项。用实际光焰网格和着色器核验静止、左右移动、升降及朝镜头移动的独立示意。
- 连续纹理图层混合：20 项真实 RenderLayer／ShaderProgram 回归。旧透明状态在第二批命中缓存后使 alpha 从约 0.475 退为 0.224；修复后的两个纹理批次保持一致，普通／Fabulous 均通过。

最新蜷腿和独立设置迁移已在隔离目录完成两包 Gradle 构建。测试外观版本为 `0.3.3+folded-flight-test-20260910`，玩法为 `0.2.2`；逐字节核对两包源码、资源和构建配置与仓库一致。启动前备份测试存档和设置，旧客户端正常保存退出。用户完成本次实机验收并同意合入。

## 本次验收关注点

以下为测试时提供的检查范围；用户的本轮验收通过不代表已穷尽多人、所有画质及第三方光影组合。

- Gameplay 模组设置页的两种能力视角、保存和返回；发动后 F5，返回时恢复原视角；外观 F9 不再显示该选项。
- 第三人称贴近地面、墙角、门与生物时，准星与实际挖掘／交互目标一致；工具侧移后仍有惯性。
- 空手光焰静止、前后移动、升降、转向和急停；不同观察方向下核心和尾部保持柔和。
- 普通／Fabulous 画质下的角、主副手、多个不同纹理物品、云层及前景遮挡；第一人称、背包与捏脸预览。
- 前腿普通悬浮放低、冲刺抬回，两种状态均蜷曲；从正面、侧面、背面及斜角检查升降、刹停和冲刺切换没有突兀关节错位。
