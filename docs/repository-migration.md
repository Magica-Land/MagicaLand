# 双仓迁移记录

## 来源与保全

- 共同基线：`a97115e702f58ef11fcdff80c4dd27b5d8409c5f`。
- 本地同仓分包：`939acdf2fb338169f24477c15b16b366d7daa1b2`。
- 远端捏脸更新：`0dd9c89`、`54a4089`、`3888892`，保留独立捏脸界面、横向分页及包名调整。
- 工作区保全：`d4786bd`，包含 16 个已跟踪文件修改和 32 个未跟踪文件。

迁移前逐一备份并核对 341 个源文件的 SHA-256，另保存完整 Git bundle、状态和文件清单。构建缓存、测试运行目录和存档不进入源码提交，也不清空原工作区的这些文件。

本仓库通过合并保留两侧历史，不强制改写远端。Gameplay 从保全快照建立独立仓库，保留原作者、许可和来源；早期完整历史仍在本仓库。

## 保护范围

模型、UV、贴图、动画、声音、Blockbench 源文件不改。外观服务端握手/协议/注视、预设、可爱标志格式不改。玩法权限、交互、背包、存档标识和彩蛋规则不改。

变化限于仓库/构建边界、Addon 的公共 API 接入及覆盖注册生命周期，不表示此前尚未验收的出窍版本已经通过游戏测试。

## 验证

2026-09-10 已执行：

- 59 个美术/运行资源文件、12 个同步相关文件、23 个玩法规则/资源文件与迁移前 SHA-256 完全一致；49 个捏脸/标题入口文件保留远端版本。
- 134 个外观生产 Java 源码使用真实 Minecraft/Fabric/GeckoLib 依赖独立类型检查通过，没有用旧外观类或测试替身掩盖源码缺失。
- 全部 26 个 Gameplay 生产源码仅依赖公开 API 类及真实开发库编译通过，编译 classpath 没有外观内部实现。
- Gameplay 的方向/飞行、彩蛋状态/顺序/食物、携带槽共 5 项无窗口回归通过，合计 727,810 条检查；隔离的真实 `0.3.0`/`0.1.0` 元数据加载与 18 个 Mixin 目标转换通过。使用当前源码编译的开发命名空间测试夹具，没有进入游戏主循环，不等于正式重映射 JAR 验收。
- 外观架构 474 项、Gameplay 架构 169 项、远控结构 77 项检查通过；API 42 项行为与 API-only 消费者/公开签名检查通过。
- 14 个 GUI 测试入口编译通过，13 个无需图形上下文的逻辑测试通过，共 1,615,146 条检查；修正了旧缩略图测试错误要求忽略镜像的过期断言，保留固定配色和源配置隔离检查。需要 OpenGL 的渲染状态测试仅编译，未运行。
- 挑染生成器只读检查通过：261 个发块、1,566 个面、114 个色区，01 未改动。
- 正式 Windows 签出会按 Git 设置转换文本换行；修正生成器将 CRLF 误报成遮罩损坏的问题。新增 LF、CRLF 和真实色区篡改回归，忽略的仅是换行差异，仍完整核对生成数据；没有重写资源。
- 两仓 6 份 Gradle 文件语法检查通过，wrapper 来自官方 Gradle 9.4.1 且带发行包 SHA-256。真实 Loom 1.16.3 验证支持 API-only JAR 的显式 remap 标志。
- 外观发布任务图 dry-run 通过，所有编译/打包/发布任务均跳过；仅生成 POM 验证坐标和依赖，无反向 Gameplay 依赖。检查时通过临时 init 脚本将插件标记指向本地已缓存的同一官方 1.16.3 模块，没有改交付配置。

尚未完整构建、实际发布 Maven 开发产物或启动游戏。Gameplay 配置检查已经到达外部 API 依赖解析，但因 `0.3.0` 尚未实际发布而停止；须按双仓说明先发布外观，不能把这次检查说成两包端到端构建成功。没有制造占位依赖来掩盖这个前置条件。

独立源码、结构和任务图检查不等于多人渲染验收。遵循 `AGENTS.md`，未经用户要求不自行构建和启动游戏。
