# 自动眼仁注视

只移动眼仁，不转动头部、镜头，不自动改变表情。默认启用；可在模组设置的游戏页关闭“眼仁自动注视”。这是本客户端的显示开关，适用于本机看到的小马，不改变其他客户端的设置。

## 目标规则

固定优先级：最近攻击过自己的实体 → 敌对实体 → 其他玩家 → 中立生物 → 友善生物。同级选最近；只有距离差不足 0.2 格时保留原目标，避免反复切换。更高优先级不受该缓冲限制。

- 范围为玩家位置到实体位置的三维球形距离 8 格，含边界，不是 8 格搜索方框。
- 最近攻击者使用原版记录的最后一个攻击者，优先权保留 100 tick（正常 TPS 下 5 秒）。记录过期后按该实体普通分类处理。
- 只考虑活着的玩家和生物，不看自己、旁观者、盔甲架、掉落物、经验球。睡眠、死亡、旁观状态的小马不选目标。
- 隐身实体一律排除，即使同队或有其他显示效果也不作为目标。另检查针对该玩家的不可见状态。
- 从被渲染玩家的原版眼部位置到目标眼部位置做方块视线检测；有遮挡就排除，不穿墙追踪。使用原版 `VISUAL` 射线形状，忽略液体。其他可见生物用碰撞箱检查遮挡；这是保守的眼部射线判定，不是像素级外形遮挡分析。
- 搜索限定在玩家头朝向左右各 100° 的前侧区域，正后方不追踪；最终眼仁仍受模型实际朝向和安全幅度约束。不是读取第三人称摄像机的画面。
- 中立生物明确以自己为攻击目标、或对自己愤怒时提升到敌对。兼顾普通攻击目标与 Brain 的 `ATTACK_TARGET` 记忆。

默认中立分类涵盖原版 `Angerable` 生物及猪灵、山羊、羊驼、行商羊驼、熊猫、海豚。其余带 `Monster` 标记的生物归敌对；其余生物归友善。蜘蛛按敌对类别处理，不额外预测白天、亮度等情境。第三方生物可能需要补充分类。

可用数据包扩展 `magicaland:gaze_hostile`、`magicaland:gaze_neutral`、`magicaland:gaze_friendly` 三个实体类型标签。最近攻击者、已确认针对自己的攻击状态优先；显式敌对标签高于友善和中立标签，友善标签可覆盖默认类别。不要同时把同一类型加入多个标签。

## 表情与移动

`assets/magicaland/expressions.json` 的表情定义增加 `gaze`：

- `auto`：允许自动注视。当前只有普通表情启用，因此使用普通眼的动作可以注视。
- `locked`：保留作者编排的眼神。向后看、向下看、下落过渡、挥手、芭蕾使用此模式。
- `off`：不添加自动眼仁移动。现有笑眼、闭眼、挤眼及独立生气眼型使用此模式。

未填写 `gaze` 的旧资源包默认锁定，避免覆盖作者眼神。一个动作引用先播后循环的两个表情时，两者都允许才会启用；因此当前下落过渡动作整段让位于原演出。修改映射不必编辑身体动画。

每个 `eye_styles` 条目增加 `gaze_limits`，单位为模型像素：

| 眼型 | 左眼向外 | 右眼向外 | 向上 | 向下 |
| --- | ---: | ---: | ---: | ---: |
| 01 | 1.20 | 1.20 | 1.05 | 1.20 |
| 02 | 1.20 | 1.20 | 0.45 | 1.20 |
| 03 | 0.60 | 0.65 | 0.85 | 1.20 |

左右眼以角色的 `left_pupil`／`right_pupil` 骨骼为准，不是观察者屏幕上的左右。默认恢复模型作者原本向内聚拢的眼神，不再向眼白中心自动平移；原有高度也保持不变。无目标且不转动视角、捏脸预览及关闭自动注视时均保留作者默认姿态。正常向外移动使用上表幅度；朝鼻梁移动最多 0.15 模型像素，随注视角度平滑增加，不再完全锁死另一只眼。03 的斜眼框与左右非对称形状独立处理；动态眼神以作者原位置为基准，不覆盖锁定的动作眼神。

水平与垂直合用椭圆限位，不在对角线叠出过大位移。每个眼型可在 `gaze_limits` 中增加 `left`／`right`，分别填写 `outward`、`up`、`down`；`outward` 表示向外水平幅度，已适配裁剪的眼型向内另受 0.15 上限约束，且不超过该眼型本身的水平幅度。省略时继承通用上限，不需要增加动画轨道。数值必须在 0–2.5 模型像素以内，单眼配置不得超过通用上限。

眼眶裁剪从父眼型骨骼的真实眼白面提取固定窗口，03 的重叠白块先求并集，02 窗口在前置眼皮处结束。先应用眼仁当前的位移、缩放和旋转，再在眼眶局部坐标中，按窗口裁掉越界的虹膜、瞳孔和高光。裁剪边缘的 UV 通过插值计算，以保留原有渐变。眨眼缩放不再反向压小注视幅度，而由固定窗口遮去越界部分。

使用原有渲染层和深度规则，不增加遮挡模型、透明遮罩或专用着色器；不改模型、UV、纹理或 Blockbench 项目。不能识别或无效的窗口会缓存回退结果，不自动居中，并使用保守的旧单侧位移与缩放余量策略（水平不超过 0.3、上 0.15、下 0.2），不把新的大幅度应用到未适配资源包。

方向响应在水平约 45°、垂直约 22° 达到满幅（斜向仍受椭圆约束）。超出窗口的部分不提交绘制，仍保留动作幅度上限，避免整只眼仁离开眼眶。

注视方向通过实际渲染矩阵换算，包含身体转向、头部动画和飞行侧倾。只在世界中的玩家模型渲染时启用；标题、捏马预览不追踪世界实体。平滑按游戏时间推进，不按渲染次数推进；目标丢失后缓慢回正。临时骨骼状态在面部绘制结束后恢复，异常退出亦恢复。

## 无目标时的转向预看

没有有效实体目标时，`PonyTurnGaze` 根据被渲染玩家头部朝向（`headYaw`）／视角俯仰（`pitch`）的转动速度，让眼仁轻微看向转身、抬头或低头的方向；输入与头部视角渲染一致，远端不会把独立的身体转向误当成抬眼方向。停止转动后经原有平滑器回正，不根据绝对头角度持续偏眼。水平最多使用当前幅度的 60%，垂直最多 35%；与实体注视共用新的双眼移动及眼眶裁剪。

实体目标严格优先：有目标时清空转向预看输出，只使用原实体投影，不把转向量相加。两种目标共用原有平滑器，切换时从已显示的位置自然过渡。作者锁定眼神、笑眼／闭眼等禁止自动注视的表情、后退回头锁定、自动注视设置及世界预览隔离仍优先，不增加目标搜索或网络消息。

先在 tick 间插值玩家朝向，再按游戏时间计算实际转动速度，远端的逐 tick 更新也不会因帧率越高而把预看幅度挤成短脉冲；重复绘制不重复推进，跨 ±180° 使用短角差。每个玩家和世界独立保存采样，重生、换世界、时间倒退、超过 5 tick 未采样、单次位置跨越 4 格或异常大幅角度跳转时清空预看和平滑状态，避免传送、恢复渲染时甩眼。此功能不是身体转向同步修复，不改变下文记录的远端身体朝向待办。

`PonyTurnGazeTest` 通过 41885 项离线检查，包含世界空间转向投影符号证明、实体存在但投影为零时仍优先、20／30／60／144 FPS 同速度结果、停止回正、重复绘制和异常重置。该预看功能的上一轮游戏已启动供用户验收。

裁剪首轮增加 `EyeApertureClipTest`（247866 项纯几何检查）及 `EyeApertureRenderTest`（315380 项真实烘焙模型检查）：覆盖 03 重叠白块并集、共享边侧面不重画、UV 插值、六只眼仁中心校正和双向移动、三种眨眼缩放、正面／侧面／背面及任意父级变换、异常资源回退和绘制后状态恢复。旧注视、表情和头部／后退锁定回归继续通过。2026-09-09 已构建首轮新版供用户实机验收；上述数量属于首轮离线验证，不代表下述观感微调已经验收。

### 2026-09-09 观感微调记录（默认位置已被后续恢复取代）

用户反馈首轮默认眼神过于分离，02 上看及 03 左右幅度偏大。本次将完全居中的校正量收回 40%：01／02 每眼相较首轮向内约 0.30 模型像素，03 约 0.289；不恢复原来的完全内聚。02 上看从 1.00 降到 0.45；03 左右水平从 1.05／1.10 降到 0.60／0.65。其他方向、双眼双向移动、眼眶裁剪、目标优先级和动作表情规则均不变。模型、UV、贴图及 Blockbench 项目不改动。

微调后通过 120 源码 Java 17 检查及 14 组离线回归；真实烘焙眼型测试更新为 331999 项，新增 60% 校正、40% 向内余量、两眼重复居中不累积、02／03 精确限位及实际九方向移动验证。该结果验证实现与裁剪边界，不替代用户对新版眼神的实机观感验收。

### 2026-09-09 恢复作者默认眼位

用户进一步反馈仍偏好原本的斗鸡眼造型。本次移除运行时眼位居中校正及其预览／注视接入，直接沿用原模型和动作轨道的默认位置；完全居中与 60% 校正两版均不再使用。保留眼眶裁剪、双眼双向移动、02 上看 0.45、03 左右 0.60／0.65，以及原有目标优先级、转向预看和表情规则。不恢复此前限制另一只眼不动的旧方案，也不修改模型、UV、贴图或 Blockbench 项目。

恢复后通过 120 源码 Java 17 检查与 14 组离线回归，其中真实眼型测试 332349 项，覆盖作者默认及非零动作轨道不被校正、重复零输入不累积、动态范围、眼眶裁剪和绘制后恢复。本次未重新构建或启动客户端，也未推送；已运行的测试版不会自动获得这次恢复。

### 2026-09-09 恢复鼻梁方向限位

已按用户随后要求构建并启动恢复作者眼位的独立客户端，供其验收。用户进一步要求仅收紧朝鼻梁方向，不再缩小正常左右移动：原来完全不许向内移动的限制改为每眼最多 0.15 模型像素，按输入比例渐进；向外、上下、原始默认眼位及裁剪保持不变。该限位尚未进入上述正在运行的测试实例。

鼻梁限位已通过 506945 项真实眼型／裁剪检查和 9827 项表情回归，并逐字比对配置确认正常左右及上下幅度完全未改。验证包含小角度输入、斜向、三种眨眼缩放及作者非零轨道；向内有少量运动，向外仍使用原幅度。

## 联机与性能

支持新版服务端时由服务端选目标，客户端再次检查范围、隐身与视线，再渲染眼仁。不接受客户端指定攻击者或注视目标。每 5 tick 重选目标，按优先顺序验证候选视线，找到可用目标即停止；每 tick 验证已有目标。目标变化发送消息，每 40 tick 补发一次。消息只含源玩家、维度、目标实体 ID 与 UUID，不逐帧发送坐标。

目标消息通过独立的 `magicaland:gaze_v1` 服务端单向通道发送给自己，以及正在追踪该玩家且支持该通道的客户端。关闭外观广播时仍能得到自己的服务端判定，但不向其他玩家广播自己的注视状态。客户端验证 ID 与 UUID 同时吻合，避免实体 ID 复用导致看错人；切换世界、断线及过期消息会清理状态。

旧版／未安装模组的服务端不具备准确的攻击者和仇恨同步，降级为客户端基于可见实体的分类与距离选择；中立实体是否正在针对自己、最近攻击者可能无法准确判定。客户端保留原版中立类型回退，不依赖旧服务端提供新增标签。不要把降级模式当成准确仇恨判断。

## 离线验证

首次离线阶段使用现有缓存依赖，对 19 个相关 Java 源码／测试文件作 Java 17 独立类型检查；该阶段没有启动游戏。随后按用户授权完成下述实际游戏验收。

- `GazePolicyTest`：1032 项断言，覆盖优先级、距离、时间窗、缓冲切换、视角、遮挡候选回退和无目标恢复。
- `GazeMessageTest`：65 项断言，覆盖目标／清空消息往返、截断、非法实体 ID、维度长度和多余数据拒绝。
- `PonyGazeMathTest`：4512 项断言，覆盖左右／上下方向、任意变换、退化矩阵、椭圆限位、帧率独立与时间回退。
- `PonyFacePoseTest`：9677 项断言，覆盖原表情维修及三眼型注视限位、仅 XY 移动、原缩放保留、异常恢复和资源配置校验。

## 2026-09-08 游戏验收

在隔离开发副本中执行完整 `runClient` 编译、启动和正常退出，Gradle 返回 `BUILD SUCCESSFUL`。单客户端连接真实内置服务端；测试辅助模组只布置场景、切换测试镜头和读取结果，不替换生产目标选择、网络消息或眼仁渲染实现。

51 组场景、875 项运行断言全部通过，保留 155 张真实游戏帧缓冲截图。人工核对三眼型对比图及正面、35°、75° 视角，没有在已测画面发现眼仁越界或明显方向错误。另通过真实设置界面关闭、开启开关，分别确认配置保存为 `false`、`true`。

- [x] 01／02／03 的左右、上下、无目标回正，目标移动与设置关闭／恢复。
- [x] 敌对优先于中立、中立优先于友善，同级选近；最近攻击者优先以及 100 tick 记录过期。
- [x] 实心墙遮挡、其他实体遮挡、隐身、隐身且发光、8 格边界及 8.05 格越界、目标移除后回正。
- [x] 狼通过攻击目标字段、猪灵通过 Brain 攻击记忆进入敌对分类；未激怒猪灵保持中立。
- [x] 三眼型保留眨眼的眼仁缩放；潜行、闭眼及作者指定的向后看／挥手眼神不叠加自动移动。
- [x] 游戏设置页实际点击开关并保存。

验收边界：最近攻击者和激怒状态由场景直接设置原版记录／目标字段，没有实测受伤或 AI 激怒流程。其他玩家只用未加入世界的原版玩家对象验证分类等级，不代表双客户端联机通过。向后看、挥手是在站姿上播放对应面部片段验证优先权，不代表完整身体动作已回归。三视角截图是固定姿态比较，不替代所有动画连续帧检查。

运行日志仍有既有 GeckoLib 几何版本和开发账号 Realms 提示；未发现本次自动注视的异常。测试后撤下辅助 JAR，原音量等 3 份设置与 48 个测试世界文件按备份逐字节恢复。83 个测试源码／资源同步项、27 个已应用文件和 8 个受保护美术资源在本轮测试期间哈希一致；只在测试结束后更新本说明，没有修改功能或美术资源。

## 同日增补：扩大幅度与单眼限位

用户验收第一版后要求扩大幅度、避免两眼一起挤向鼻梁，参数已调整为上表。原 51 组记录对应第一版，不应当作新参数的截图证据。

新一轮在相同隔离开发副本中完成 36 组实机场景、828 项运行断言，保存 108 张实际帧缓冲截图。三眼型分别检查居中、左右、上下、四个斜向、目标移动、关闭开关、闭眼；每组采集正面、35°、75° 视角。单独记录两眼位移，确认水平只有目标侧向外移动，另一只不向鼻梁偏移；上下均可移动，原眨眼缩放不变。已检查当前极限位置拼图，没有在已测画面发现明显越过眼框的眼仁或高光。

独立 Java 17 检查仍覆盖 19 个文件；四组测试分别通过 1032、65、4512、9827 项断言（共 15436 项），新增真实眼仁尺寸、1.07 倍眨眼回弹安全余量、非目标侧保持水平位置、单眼配置校验。没有改动目标优先级、网络协议、作者表情分配或美术资源；联机与其他未覆盖动作仍保留下列待验收项。

## 同日增补：真实双客户端验收

使用两个独立 JVM、独立游戏目录及不同离线开发 UUID，访客通过真实 TCP 连接主机的内置服务端。监听地址经系统连接表核对为 `127.0.0.1`，没有向局域网或公网开放，也没有连接 VPS。此轮不是独立服务端、真实账号登录或高延迟网络测试。

共取得 22 组双端场景记录、44 张实际帧缓冲截图。其中 21 组达到各自检查条件，1 组保留明确的朝向差异，不能概括为“多人完全通过”。

- 已通过：真实玩家加入与优先级、敌对及最近攻击者优先、攻击记录过期、隐身玩家／敌人、实心墙、8 格出入、实体追踪卸载／恢复、外观广播关闭／恢复、访客本地眼神开关、眼型配置同步。
- 已通过：下界目标清空和新鲜渲染帧回正、返回主世界后恢复、真实断线重连。重连后实体 ID 从 56 变为 126，仍正确关联 UUID；断线回调结束后注视目标和快照均为 0。
- 同姿势对照通过：同一角色在自己客户端与另一客户端的眼仁偏移收敛；原缩放、旋转和深度不被注视叠加改变，水平位移仍只向眼框外侧。

### 待解决：传送后的远端身体朝向差异

快速移动／卸载再进入追踪后，访客本地 `bodyYaw=180°`，主机观察该访客时 `bodyYaw=-130°`，等效差 50°；两端头朝向均约 180°，目标均为主机。因为现有小马整体按 `bodyYaw` 旋转，这时主机看到的是侧身，眼仁投影回中；访客自己仍看到侧向注视。截图与数值均复现，不是目标消息选错人。

仅在诊断控制组将两端身体朝向对齐后，眼仁数值及画面恢复一致。对齐逻辑只在测试辅助模组中，未进入正式代码；不能拿控制组代替自然转向的联机验收。现有按 `bodyYaw` 旋转的实现也存在于加入自动注视前的提交中，但尚未用旧版双开复现，因此目前只能判定它暴露了既有朝向路径的问题，不能证明所有触发条件都与本次改动无关。正常步行与手动转身的复现率仍待补验。

- [ ] 先用自然移动、转身和传送区分本地强制摆姿与原版远端身体插值的影响，再决定是否修正身体／头部朝向基准。不要直接强制两端整体同向，也不要通过新增动作补偿掩盖。

### 测试限制及工程保护

早期轮次遇到截图目录缺失和 Windows 原子写入占用，已修正辅助工具，并要求新鲜帧与实际截图落盘后才记录完成。原下界场景没有取得有效眼仁画面；续测改为无遮挡平台，已取得新鲜回正帧。这些失败记录保留，不计入通过项。

两次 JVM 原生崩溃落在 `glfw.dll`，其中一次尚未进入世界。现有 Java 25 测试环境采用 `-XX:TieredStopAtLevel=1`、垂直同步及调整限帧设置后，完成后续测试并正常退出；这些参数仅用于隔离测试实例。尚未证明原生崩溃根因，不代表默认启动参数的双开稳定性已经解决，也不是性能验收。

本轮未修改功能源码或美术资源。83 个测试源码／资源同步项、原已应用的 27 个文件、8 个受保护美术文件，在更新本说明前均通过哈希核对。原 3 份设置和 48 个存档文件逐字节未变；双客户端正常退出，回环监听已释放，辅助 JAR 不在正式资源或原测试运行目录中。未提交、未推送。

## 后续待验收

- [x] 第二名真实玩家加入混合优先级场景；双客户端目标同步、新加入观察者、外观广播关闭。自然朝向下的画面一致性仍有上述待解决项。
- [ ] 玻璃／栅栏等非实心方块、目标正常死亡及实际伤害／激怒流程。
- [ ] 睡眠、完整挥手／向后看／飞行侧倾等动作的连续画面。
- [x] 切换维度、返回及断线重连的目标清理／恢复。
- [ ] 传送后的自然身体朝向一致性、旧服务端降级、独立服务端及网络延迟。
- [ ] 游戏资源重载全流程、密集生物场景性能。

这些是新功能的验收项，与旧表情系统已有的游戏验收记录分开维护。
